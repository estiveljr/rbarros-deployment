# =============================================================================
# RBarros Application Environment Configuration
# =============================================================================
# Copy this file to .env and modify values as needed
# For local development, most defaults below should work out of the box

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=database
DB_USER=rbarros_dev
DB_PASSWORD=dev_password_123
DB_NAME=rbarros_development
MYSQL_ROOT_PASSWORD=root_dev_password_123

# =============================================================================
# APPLICATION SECRETS
# =============================================================================
# ⚠️  IMPORTANT: Change these in production! These are for development only
SECRET_KEY=dev-jwt-secret-key-for-local-development-minimum-32-chars
SECRET_KEY_REFRESH_TOKEN=dev-refresh-token-secret-for-local-development-32-chars

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================
# For local development, you can use placeholder values
# Replace with real values when you need to test email functionality
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
WEBHOOK_SECRET=dev-webhook-secret-for-local-testing

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
NODE_ENV=development
PORT=3000

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# For local development
VUE_APP_API_URL=http://localhost:3000

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# When deploying to production:
# 1. Change NODE_ENV to 'production'
# 2. Use strong, unique passwords for database
# 3. Generate secure random strings for JWT secrets (min 32 characters)
# 4. Use real SendGrid API key
# 5. Set VUE_APP_API_URL to your production domain
# 6. Use GitHub secrets for CI/CD deployment 